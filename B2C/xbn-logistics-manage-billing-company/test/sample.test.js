import assert from 'power-assert';

describe('Sample test cases', e => {



    it('hello test', async () => {
        
        const name = 'test';
	const name1 = 'test';
        assert(name === 'test');
    });
});
